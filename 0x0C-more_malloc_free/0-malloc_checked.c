#include "main.h"

/**
 * malloc_checked - function to allocate memory in C
 * @b: integer type
 * Return: nothing
 */

void *malloc_checked(unsigned int b);
{
	void *ptr = b;

	if (*ptr == NULL)
	{
		return (98);
	}
}
