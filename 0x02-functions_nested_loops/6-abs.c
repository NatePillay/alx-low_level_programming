#include "main.h"
/**
 *_abs - this function will be used to get absolute value
 *@int: this is the data type of the answer we want returns
 *Returns: _abs number if success 0 if nothing
 */
int _abs(int)
{
	int n;

	if (n < 0)
	{
		return (n * (-1));
	}
	else
		return (n);
}
