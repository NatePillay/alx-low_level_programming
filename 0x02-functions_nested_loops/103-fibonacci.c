#include <stdlib.h>
#include <stdio.h>
/**
 * Description: main - we going to sum up all even numbers
 *
 * Return: we only want even numbers
 */
int main(void)
{
	long int i, n, sum;
	long int t1 = 1, t2 = 2;
	long int term = t1 + t2;

	n = 33;

	for (i = 1;  i <= 33; ++i)
	{
		if (i < 40000000 && (i % 2) == o)
		{
			sum = i + sum;
		}
		term = t1 + t2;
		t2 = t1;
		t2 = term;
	}
	return (0);
}
