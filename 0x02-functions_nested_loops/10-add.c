#include "main.h"
/**
 * add - adds the two together in function
 * @x: first number to be added
 * @i: second number to be added
 * Return: result of two integers adds.
 */
int add(int x, int i)
{
	int x;
	int i;

	return (x + i);
}
