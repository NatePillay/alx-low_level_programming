#include "main.h"
/**
 * print_numbers - function to return numberz
 *
 * Return: 0 if success
 */

void print_numbers(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}

